import 'package:flutter/material.dart';

class AppColors {
  //  Colores principales
  static const primary = Color(0xFFFF6B35);
  static const primaryContainer = Color(0xFFFFF0EB);
  static const secondary = Color(0xFF202124);

  //  Colores para mensajes de estado
  static const success = Color(0xFF4CAF50);
  static const successContainer = Color(0xFFE6F4EA);

  static const error = Color(0xFFE53935);
  static const errorContainer = Color(0xFFFDECEA);

  static const warning = Color(0xFFFFB300);
  static const warningContainer = Color(0xFFFFF8E1);

  static const info = Color(0xFF1976D2);
  static const infoContainer = Color(0xFFE8F0FE);

  // Colores de fondo y superficie
  static const background = Color(0xFFF8F9FA);
  static const surface = Colors.white;
  static const outline = Color(0xFFE0E0E0);

  //  Colores para texto
  static const onBackground = Color(0xFF1A1C1E);
  static const onSurface = Color(0xFF202124);
  static const onSurfaceVariant = Color(0xFF757575);
  static const onPrimary = Colors.white;
}
